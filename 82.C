#include<stdio.h>
int main()
{
double l,b,total;
printf("\n enter the length of the farm:");
scanf("%lf",&l);
printf("\n enter the breadth of the farm:");
scanf("%lf",&b);
total=l*b;
printf("the  area is:%lf",total);
return 0;
}
