#include<stdio.h>
int main()
{
float l,b,h,vol,totalarea;
printf("\n enter the length breadth and height:");
scanf("%f%f%f",&l,&b,&h);
vol=l*b*h;
tsa=2(l*b)+2(l*h)+2(h*b);
printf("\n volume:%f",vol);
printf("\n total suraface area:%f",totalarea);
return 0;
}
